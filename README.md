# mailhog learning

> nodejs application use mjml&&email-templates&&nodemailer

## How to running

* start mailhog server

```code
docker-compose up -d
```

* start nodejs app

```code
cd app
yarn
yarn start
```

* view UI result

```code
open http://localhost:8025
```